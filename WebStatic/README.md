This directory houses the scripts required to run a TAZI server.
The 'appengine' directory is used to host a local server. All files required
to run the server are copied via the Makefile in this directory
